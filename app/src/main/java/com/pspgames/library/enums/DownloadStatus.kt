package com.pspgames.library.enums

enum class DownloadStatus {
    COMPLETED,
    DOWNLOADING,
    FAILED,
    CANCELED,
    ENQUEUE,
    REMOVED,
    UNZIPPED,
    PAUSED,
    UNZIPPING,
    RESUMED
}